"use client";

// This component previously tracked page views with Redis
// Now it's a placeholder that doesn't track anything
export const ReportView: React.FC<{ slug: string }> = ({ slug }) => {
	// No tracking functionality
	return null;
};
